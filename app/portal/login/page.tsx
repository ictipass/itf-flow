import Link from "next/link";
import { portalLoginAction } from "@/app/portal-actions";
export default async function PortalLogin({ searchParams }: { searchParams: Promise<{ error?: string; verified?: string }> }) { const p = await searchParams; return <main className="portal-page"><div className="portal-card"><span className="eyebrow">ITF stakeholder portal</span><h1>Sign in</h1>{p.verified ? <p className="notice success">Email verified. You can sign in.</p> : null}{p.error ? <p className="notice error">Sign-in was unsuccessful. Check verification and credentials or wait before retrying.</p> : null}<form action={portalLoginAction} className="grid"><label className="field"><span>Email</span><input type="email" name="email" required /></label><label className="field"><span>Password</span><input type="password" name="password" required /></label><button className="btn">Sign in</button></form><p><Link href="/portal/register">Create stakeholder account</Link> · <Link href="/">ITF Flow home</Link></p></div></main>; }
