import Link from "next/link";
import { portalRegisterAction } from "@/app/portal-actions";
export default async function PortalRegister({ searchParams }: { searchParams: Promise<{ error?: string; status?: string }> }) { const p = await searchParams; return <main className="portal-page"><div className="portal-card"><span className="eyebrow">Verified stakeholder access</span><h1>Create account</h1>{p.status ? <p className="notice success">If this address can be registered, a verification link has been queued. Check your email.</p> : null}{p.error ? <p className="notice error">Review the fields. Passwords require at least 12 characters.</p> : null}<form action={portalRegisterAction} className="grid"><label className="field"><span>Full name</span><input name="name" required /></label><label className="field"><span>Organization</span><input name="organizationName" required /></label><label className="field"><span>Email</span><input name="email" type="email" required /></label><label className="field"><span>Phone</span><input name="phone" /></label><label className="field"><span>Password</span><input name="password" type="password" minLength={12} required /></label><button className="btn">Create and verify account</button></form><p><Link href="/portal/login">Already registered? Sign in</Link></p></div></main>; }
